# 주식 매매일지

`index.html`을 브라우저에서 열어 매매일지를 관리합니다. 데이터는 브라우저에 자동 저장되며, `CSV 내보내기`로 시트 파일을 갱신할 수 있습니다.

## 구성

- 왼쪽/첫 번째 표: 현재 보유 중인 주식
- 오른쪽/두 번째 표: 매매 완료한 주식
- 판매날짜와 판매가격이 있으면 매매 완료로 분류됩니다.
- 판매날짜와 판매가격이 비어 있으면 현재 보유 중으로 분류됩니다.

## 입력 항목

- `Buy Date`: 구매날짜
- `Ticker`: 종목
- `Market`: 시장
- `Side`: 구분
- `Buy Price`: 매매단가
- `Quantity`: 수량
- `Fee`: 수수료
- `Exchange Rate`: 환율
- 미국 시장 입력 시 인터넷에서 가져온 USD/KRW 환율이 자동 적용됩니다.
- `Current Price`: 현재가
- `Sell Date`: 판매날짜
- `Sell Price`: 판매가격
- `Net Profit`: 최종 순수익
- `Return %`: 매매 당시 주가 대비 수익률

## 계산 방식

- 보유 중: `(현재가 - 매매단가) * 수량 * 환율 - 수수료`
- 매매 완료: `(판매가격 - 매매단가) * 수량 * 환율 - 수수료`
- 수익률: `최종 순수익 / (매매단가 * 수량 * 환율) * 100`

## 환율과 계좌 연동

- USD/KRW 환율은 Frankfurter 공개 API를 사용합니다.
- 홈페이지의 `현재 환율` 카드를 클릭하면 환율 그래프를 볼 수 있습니다.
- 환율 그래프는 일별, 월별 평균, 년도별 평균으로 과거 환율을 조회합니다.
- 한국 시장은 환율 1로 계산합니다.
- 삼성증권 계좌는 브라우저에서 직접 로그인 정보를 다루지 않습니다.
- 공식 API, 오픈뱅킹, 마이데이터, 사내 백엔드 등 합법적인 서버가 있으면 `삼성증권 계좌 연동` 패널의 API 주소로 보유 종목을 가져올 수 있습니다.
- 연동 API 응답은 `holdings` 배열을 반환해야 합니다.
